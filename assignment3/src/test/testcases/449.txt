
		Var: x = 1, y = 1.0, z;
		Function: foo
		Body:
		    z = float_to_int(x + int_of_float(y +. float_to_int(x + int_of_float(y)))) >=. 2.0 *. (float_to_int(x) +. y);
		    Return z;
		EndBody.
		Function: main
		Body:
		    Var: a;
		    a = z;
		    Return a || foo();
		EndBody.
		